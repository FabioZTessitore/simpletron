/*class Memory {
  constructor (size) {
    this.size = size;
    this.reset();
  }

  reset() {
    this.m = new Array(this.size).fill(0);
  }

  set(index, value) {
    this.m[index] = value;
  }
}

export default Memory;
*/
